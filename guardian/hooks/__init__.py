"""Guardian hook scripts invoked by Claude Code.

Placed under guardian/ rather than .claude/ because the project gitignores .claude/.
Wire them into .claude/settings.json with absolute or ${CLAUDE_PROJECT_DIR} paths.
"""

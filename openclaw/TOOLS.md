# Tools Reference

**There is NO MCP server. MCP was removed.** All tools are native Python functions called directly by the agent runtime in `cli/agent_tools.py`.

See `openclaw/AGENT.md` for the full tool list and usage instructions.

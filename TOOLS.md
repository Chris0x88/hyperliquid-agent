# Tools

**There is NO MCP server.** MCP was removed in v4 (2026-04-05).

All agent tools are Python functions in `cli/agent_tools.py` and `common/tools.py`, called directly by the embedded agent runtime.

See `openclaw/AGENT.md` for the full tool list.
